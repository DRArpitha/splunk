[module]
# The JavaScript name of the module
className = Splunk.Module.SavedSearches

# The module class to subclass from
superClass = Splunk.Module

description = this module has a custom python implementation that it uses to display the list of saved searches for the current user.